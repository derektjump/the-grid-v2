# Django Settings
DJANGO_SETTINGS_MODULE=the_grid.settings.local
DJANGO_SECRET_KEY=your-secret-key-here

# Database Configuration (PostgreSQL)
# For local development, set these to use local PostgreSQL
# For Azure, these will be configured in Application Settings
DB_NAME=the-grid-v2
DB_USER=your-db-username
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# For Azure production, use:
# DB_HOST=your-server.postgres.database.azure.com
# DB_USER=your-admin-username@your-server

# Production Settings (Azure only)
# DEBUG=False
# ALLOWED_HOSTS=your-app-name.azurewebsites.net
