# Django and core dependencies
Django>=5.0,<5.1
psycopg[binary]  # PostgreSQL adapter (for production)

# Production server
gunicorn>=21.2.0
whitenoise>=6.6.0  # Static file serving for production

# Environment variable management
python-dotenv>=1.0.0

# Azure AD / Entra ID authentication via OpenID Connect
mozilla-django-oidc>=4.0.0

# TODO: Azure SDK (if needed for blob storage, app insights, etc.)
# azure-storage-blob>=12.19.0
# azure-identity>=1.15.0

# Optional: Django Debug Toolbar (for local development)
# Uncomment if you want to use it
# django-debug-toolbar>=4.2.0

# Optional: HTMX (if using for interactive UI)
# django-htmx>=1.17.0
